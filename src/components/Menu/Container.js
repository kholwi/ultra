import React from 'react';

const Container = () => {
    return (
        
        <div class="font-bold text-xl mb-2 p-10">
            <div class="box-border h-45 w-40 p-4 border-4 ...">
                Since the result of using the box-sizing

                Applying this to all elements is safe and wise:
            </div>
            <div class="box-border h-45 w-40 p-4 border-4 ...">
                Since the result of using the box-sizing

                Applying this to all elements is safe and wise:
            </div>
        </div>
    );
}

export default Container
